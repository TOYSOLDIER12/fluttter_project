import 'package:flutter/material.dart';

class Cardsview extends StatelessWidget {
  const Cardsview({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("Images/amogus.gif"),
    );
  }
}